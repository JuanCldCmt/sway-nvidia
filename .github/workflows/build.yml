name: Meson build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: sudo apt build-dep sway
      - name: Meson build
        run: meson build/
      - name: Ninja build
        run: ninja -C build/
